extends layout/main.pug
include mixins/video

block content
     .videos
        each item in videos 
            +video({
              id:item.id,
              title:item.title,
              views:item.views,
              videoFile:item.fileUrl
            })